/* signal.h -- Q9-Werkzeugkette, bewusst minimal (s. stddef.h/string.h).
 *
 * Local interface; it does not copy Microware's copyrighted DEFS/signal.h.
 * Q9-Tools/System/sleep.c currently includes only this guard and does not use
 * signal()/SIG*. The header is intentionally empty; add handling when needed.
 */
#ifndef Q9_SIGNAL_H
#define Q9_SIGNAL_H

#endif
