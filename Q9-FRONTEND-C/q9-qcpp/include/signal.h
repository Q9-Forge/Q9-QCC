/* signal.h -- Q9-Werkzeugkette, bewusst minimal (s. stddef.h/string.h).
 *
 * Eigene Fassung, KEINE Uebernahme von Microwares DEFS/signal.h (Microware-
 * Copyright). Q9-Tools/System/sleep.c bindet diesen Header bisher nur ein,
 * ohne signal()/SIG* tatsaechlich zu benutzen -- deshalb bewusst LEER (nur
 * die Wache). Sobald ein Werkzeug echte Signalbehandlung braucht, kommt sie
 * einzeln dazu, nicht auf Vorrat.
 */
#ifndef Q9_SIGNAL_H
#define Q9_SIGNAL_H

#endif
